export const environment = {
  production: true,
  apiUrl: 'https://minesweeper-backend-grd4.onrender.com/api/game' // This will be set dynamically by the CI/CD pipeline (e.g., Render.com) during production build
};
